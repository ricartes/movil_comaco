// src/js/mixins/volumen.js
import {
    homologarUnidadMedida, applyTotalesFromTotalVolumen, getVolumenByUM,
    getUM, getValorByUM
} from '@/js/Utils/volumen';

export default {
    methods: {
        homologarUnidadMedida, applyTotalesFromTotalVolumen, getVolumenByUM, getUM,
        getValorByUM
    },
};
